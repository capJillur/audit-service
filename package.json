{
  "name": "page-pulse",
  "version": "1.0.0",
  "description": "Production-grade URL audit service — validation, timeouts, concurrency limits, caching, rate limiting, and structured logging.",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage",
    "lint": "node -e \"console.log('lint placeholder - wire up eslint if desired')\""
  },
  "keywords": ["url-audit", "seo", "express", "api"],
  "author": "Md Jillur Rahman",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
